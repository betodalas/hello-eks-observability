apiVersion: v1
kind: Service
metadata:
  name: hello-app
  labels:
    app.kubernetes.io/name: hello-app
spec:
  selector:
    app.kubernetes.io/name: hello-app
  ports:
    - name: http
      port: 80
      targetPort: http
