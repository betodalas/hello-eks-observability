apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: hello-canary
  namespace: argocd
  annotations:
    argocd-image-updater.argoproj.io/image-list: hello-canary=207131866724.dkr.ecr.us-east-1.amazonaws.com/hello-observability-prod/hello-canary
    argocd-image-updater.argoproj.io/hello-canary.update-strategy: newest-build
    argocd-image-updater.argoproj.io/write-back-method: git:secret:argocd/git-creds
    argocd-image-updater.argoproj.io/write-back-target: kustomization
    # kustomization.yaml's images[].name is the short alias "hello-canary", not the
    # full ECR path used above. Without this, write-back matches by the full
    # image name and appends a second (ineffective) images entry instead of
    # updating the existing one's newTag.
    argocd-image-updater.argoproj.io/hello-canary.kustomize.image-name: hello-canary
    argocd-image-updater.argoproj.io/git-branch: main
spec:
  project: default
  source:
    repoURL: https://github.com/betodalas/hello-eks-observability.git
    targetRevision: main
    path: gitops/apps/hello-canary
  destination:
    server: https://kubernetes.default.svc
    namespace: hello-canary
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
