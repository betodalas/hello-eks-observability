apiVersion: v1
kind: Service
metadata:
  name: hello-canary
  labels:
    app.kubernetes.io/name: hello-canary
spec:
  selector:
    app.kubernetes.io/name: hello-canary
  ports:
    - name: http
      port: 80
      targetPort: http
